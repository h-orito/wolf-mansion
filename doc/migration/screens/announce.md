# 画面: お知らせ (公告)

> 関連する情報ページ: [rule.md](rule.md) / [about.md](about.md) / [faq.md](faq.md) / [practice.md](practice.md)。

## 概要

- **URL (既存)**: `GET /announce`
- **テンプレート**: `announce.html` (約 965 行 / 56KB)
- **担当 JS**: なし (`<script>` 読み込みなし)
- **Controller**: `AboutController.announce` (`AboutController.kt:14-15`) — model 注入なし、`"announce"` を返すのみ
- **対象ユーザー**: 全員 (公開)

## 1. 機能 / 出来ることリスト

- 更新履歴・お知らせの閲覧のみ (**完全静的**、更新履歴の長い羅列)

## 2. 表示要素・UI 状態

- 日付ごとの更新内容 (役職追加・不具合修正・仕様変更等) のリスト。約 965 行の静的 HTML

## 3. 呼び出す API エンドポイント

| メソッド | パス | 用途 |
|---|---|---|
| GET | `/announce` | お知らせページ (SSR、静的) |

## 4. 既存 JS の挙動

- なし (静的、JS 読み込みもなし)

## 5. 権限による分岐

- なし (全員同一・公開)

## 6. 認可マスク

- なし

## 7. 視覚比較

- 既存 `:8091/wolf-mansion/announce`

## 8. 関連 e2e ケース候補

- [ ] お知らせページ smoke 表示 (リンク切れ・大崩れがないか)

## メモ / 移行時の注意

- 現状は更新履歴をテンプレートにハードコード。移行時は **現状踏襲** (静的ページとして移植) でよい
- 今後の更新運用 (ハードコード継続 or データ化/CMS 化) は **モダナイズフェーズ**で要検討。950 行超のため、移植時はメンテ性の観点でデータ駆動化を一案として残す
- 画像・リンクは静的アセット (frontend public/ へ移管、[06-infra-deploy.md](../06-infra-deploy.md))

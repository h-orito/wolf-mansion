# 画面: イントロ (人狼館の事件簿村ルール紹介)

## 概要

- **URL (既存)**: `GET /intro`
- **テンプレート**: `src/main/resources/templates/intro.html`
- **担当 JS**: `index.js` (読み込みのみ、固有挙動なし)
- **Controller**: `AboutController` (`GET /intro`)
- **対象ユーザー**: 全員 (公開・静的説明)

## 1. 機能 / 出来ることリスト

- 「人狼館の事件簿村」ルールの紹介 (読み物)
- ルールページ (`/rule`) への導線

## 2. 表示要素・UI 状態

- 説明テキスト (本サイトのルール紹介)
- **キャラ会話形式の解説**: `fragments/flagment-message` フラグメントで、キャラ (jimuzon / riza 等) の吹き出しメッセージを並べて説明
- ルールへのリンク

## 3. 呼び出す API エンドポイント

| メソッド | パス | 用途 | 呼び出し元 |
|---|---|---|---|
| GET | `/intro` | 静的説明ページ | SSR |

## 4. 既存 JS の挙動

- `index.js` を読み込むが、この画面固有の挙動はなし

## 5. 権限による分岐

| 権限 | 見え方・できること |
|---|---|
| 全員 | 同一 (公開) |

## 6. 認可マスク

- なし

## 7. 視覚比較

- 既存 `:8091/wolf-mansion/intro`。吹き出し会話のレイアウト

## 8. 関連 e2e ケース候補

- [ ] ページ表示 (読み物なので smoke 程度)

## メモ / 移行時の注意

- 内容はほぼ静的。React では静的コンテンツ + 共通の「メッセージ吹き出し」コンポーネントで表現
- **`fragments/flagment-message`** フラグメント (引数: type, text, charaName, 反転フラグ) は **村画面のメッセージ表示でも使う共通部品**。React コンポーネント化の対象 (step-0.7 と連携)
- キャラ画像は外部 URL (キャラチップ) 参照
- About / FAQ / Announce / Rule 等の静的系は step-0.4 でまとめて調査

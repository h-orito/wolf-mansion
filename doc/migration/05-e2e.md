# 05. E2E

## 方針

- **`e2e/` 配下**に実装する
- **Playwright** を使用
- パッケージマネージャは **pnpm**
- **ローカルでの動作確認専用**。CI では実行しない (走らせる前提で設計しない)
- 機能の細かい調査により e2e テストケースも設計し、細かくレビューしていく
  - frontend の画面ごとの調査結果と連動して、必要なテストケースを洗い出す
  - 画面ごとの詳細は `doc/migration/screens/<screen>.md` に集約し、そこから対応ケースを引く

## 実行環境

### DB
- **ローカル開発環境の DB とあいのり**で良い (専用 DB やリセット手順を作り込まない)
- 既存実装 (`http://localhost:8091/wolf-mansion/`) も同じローカル DB を共有しているため、e2e の結果が既存画面側にも見える / 逆も成立
- テスト用ユーザーは既存の `local_test_credentials.md` のものを利用 (ローカル DB の全プレイヤーは password `testuser`)

### Port
- backend / frontend は **通常起動と別ポートで動かせる**ようにする
  - 例: 通常 backend 8089 / frontend 5173 → e2e 時 backend 18089 / frontend 15173 など
  - playwright.config.ts 側で webServer 起動 + baseURL を切り替え
  - 具体的なポート割り当ては別途確定

### スクリーンショット
- 保存先は **`e2e/` 配下の専用ディレクトリ** (例: `e2e/test-results/` `e2e/playwright-report/` 等、Playwright デフォルト)
- **`.gitignore` に追加**して git 管理外にする
- (参考) Claude Code の playwright MCP から撮るスクショは引き続き `.playwright-mcp/` に置く運用 (既存ルール踏襲)

### Daychange
- バックエンドに **「日付を更新する」デバッグ機能**が存在するため、これを叩いて村の日付進行を制御する
- 時刻操作や専用エンドポイントの新設は不要

## 想定するスコープ

- 認証 (ログイン / ログアウト / refresh)
- 主要画面の表示確認 (smoke)
- 村のライフサイクル (Prologue → Progress → Epilogue → Finished, 廃村)
- 発言種別の出し分け、表情差分、装飾タグ、アンカー、秘話、返信 等
- 投票 / 能力 / コミット
- 認可マスク (足音、死亡理由など)
- creator / admin 操作 (kick, 廃村, 強制退村, etc.)
- **外部公開済み API の挙動ピン留め** ([02-backend.md](02-backend.md) 参照)

## 未確定事項 / 要調査

- [ ] e2e 用ポート割り当て (backend / frontend / Playwright config)
- [ ] e2e 実行時に backend / frontend をどう立ち上げるか (Playwright `webServer` 機能 / 手動起動前提 / pnpm script)
- [ ] テスト間の DB 状態が他のテストに引きずられない設計 (= テスト分離方針)、もしくは "あいのり前提で順序依存を許容" するのか
- [ ] フィクスチャ / ヘルパーの設計 (`e2e/fixtures/` `e2e/helpers/`)
- [ ] デバッグ用「日付を更新する」機能の **エンドポイント / 権限要件** の確認
- [ ] スクリーンショット保存先ディレクトリ名の確定 (`e2e/test-results/` / `e2e/.screenshots/` / 他) と `.gitignore` 追加
- [ ] 失敗時 trace / video の保存方針 (デフォルトのまま / on-failure / 取らない)

# サプライチェーン対策 (frontend/.npmrc と同方針): 依存の install ライフサイクル
# スクリプトをデフォルトで実行しない。Playwright の browser バイナリは postinstall
# ではなく `pnpm exec playwright install chromium` で別途取得する (README 参照)。
ignore-scripts=true
